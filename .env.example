# Amazon Bedrock
BEDROCK_ENABLED=false
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=global.anthropic.claude-haiku-4-5-20251001-v1:0

# Live opportunity discovery
# Add one or more public Greenhouse board tokens, comma-separated.
GREENHOUSE_BOARD_TOKENS=
# South African examples discovered during development:
# GREENHOUSE_BOARD_TOKENS=takealotgroup,impact,bashdotcom,offerzen

# Local AWS credentials are normally resolved by the AWS SDK credential chain.
# Do not commit real credentials to GitHub.
# If you must use environment variables locally, keep them in .env.local only:
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_SESSION_TOKEN=...   # only when using temporary credentials
